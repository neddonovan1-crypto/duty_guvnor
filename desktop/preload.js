/* Bridge between the game page and the desktop shell. Empty for now:
 * the save-file shim (issue #9) and achievement triggers (issue #10)
 * will expose their narrow APIs here via contextBridge. */
'use strict';
